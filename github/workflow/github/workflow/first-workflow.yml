name: First Workflow
on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: ehco a string
        run: echo "Hello, World!"

      - name: Multiline Command
        uses: |
          node -v
          npm -v
